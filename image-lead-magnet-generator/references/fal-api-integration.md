# FAL API Integration (DEPRECATED — see image-generation-backends.md)

**Tato reference byla nahrazena.** Veškerý obsah o FAL endpoint, authentication, request/response, rate limits a costing byl přesunut a rozšířen do:

→ **`{baseDir}/references/image-generation-backends.md`**

Nový soubor pokrývá:
- FAL API (test fáze) — kompletní integration
- OpenClaw native `image_generate` (production cíl)
- Backend selection logic
- Migration plán FAL → OpenClaw
- Fallback chain

Přesměruj sem všechny odkazy.
